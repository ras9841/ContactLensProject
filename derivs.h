#ifndef _DERIVS_
#define _DERIVS_

#include <stdlib.h>

// 1st derivatives
double c_diff1(double **, size_t, size_t, char, double);
double f_diff1(double **, size_t, size_t, char, double);
double b_diff1(double **, size_t, size_t, char, double);

#endif
