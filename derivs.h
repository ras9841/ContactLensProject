#ifndef _DERIVS_
#define _DERIVS_

#include <stdlib.h>

// 1st derivatives
double c_diff1(double **, char, size_t, size_t, double);
double c_diff2(double **, char, size_t, size_t, double);
double f_diff1(double **, char, size_t, size_t, double);
double b_diff1(double **, char, size_t, size_t, double);

#endif
